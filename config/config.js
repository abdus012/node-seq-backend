const config = {
  storage: {
      local: {
        username: "myuser",
        host: "localhost",
        database: "employee",
        password: "mypass",
        port: 3310,
        dialect: "postgres",
      },
    },
};

module.exports = config;
